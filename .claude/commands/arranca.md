---
description: Arranque de sesión — orienta y resume dónde quedamos
---

Estás retomando el proyecto **Gestor de Portfolio y Operaciones** (repo
`Registro-de-Operaciones`; las memorias viejas lo llaman "Diario de Trading").
El `ESTADO.md` (si existe) y las memorias (`MEMORY.md` + archivos) YA están
auto-cargados en contexto — no hace falta pedir que se carguen.

Hacé esto, en orden, sin preguntar antes:
1. Releé el titular del `ESTADO.md` (la "Última actualización") y el
   índice `MEMORY.md`.
2. Confirmá en UNA línea que el contexto está cargado.
3. Resumí en 3-5 bullets **dónde quedamos**: lo último que se hizo + lo que
   quedó pendiente/próximo.
4. Preguntá en qué trabajamos hoy. Si en el mismo mensaje ya te dieron la
   tarea, saltá la pregunta y arrancá.

Reglas (recordatorio): español para conversar, sin emojis, sin asumir
emociones, respuesta incómoda primero. Todo lo ESCRITO (código, nombres,
commits, memorias) en inglés salvo que ya esté en español en el proyecto
(este repo documenta y comenta en español — mantené esa convención). Git
PR-based, **no auto-commit** salvo pedido explícito.
